{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist", "css/generated.css", ".vercel"]
    },
    "types:check": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
